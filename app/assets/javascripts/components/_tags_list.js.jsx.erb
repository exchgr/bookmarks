var TagsList = React.createClass({
  render: function() {
    var tagNodes = this.props.tags.map(function(tag) {
      return(
        <li key={tag.id}>
          {tag.name}
        </li>
      )
    });

    return (
      <ul className="tags">
        {tagNodes}
      </ul>
    );
  }
});
