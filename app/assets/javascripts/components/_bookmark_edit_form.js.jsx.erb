var BookmarkEditForm = React.createClass({
  render: function() {
    return (
      <BookmarkForm
        headerText="Edit bookmark"
        displaying={this.props.displaying}
        toggleDisplaying={this.props.toggleDisplaying}
        onSubmit={this.props.onSubmit}
        data={this.props.data}
      />
    );
  },
});
