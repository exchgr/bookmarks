var BookmarkEditForm = React.createClass({
  renderedForm: function() {
    return $(".rendered-bookmark-edit-form-" + this.props.id);
  },

  render: function() {
    return (
      <BookmarkForm
        className={"rendered-bookmark-edit-form-" + this.props.id}
        displaying={this.props.displaying}
        toggleDisplaying={this.props.toggleDisplaying}
        onSubmit={this.props.onSubmit}
        renderedForm={this.renderedForm}
        id={this.props.id}
        title={this.props.title}
        url={this.props.url}
      />
    );
  },
});
